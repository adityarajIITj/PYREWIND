[build-system]
requires = ["hatchling>=1.18"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["pyrewind"]

[project]
name = "pyrewind-dev"
version = "0.3.0"
description = "Step-by-step execution traces, assisted replay, interactive terminal TUI scrubber, and automated root-cause diagnostics for Python functions"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [{ name = "Aditya Raj", email = "b25bs1020@iitj.ac.in" }]
keywords = ["tracing", "execution", "debugging", "analysis", "replay", "tui", "diagnostics", "root-cause"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Environment :: Console",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Natural Language :: English",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Topic :: Software Development :: Debuggers",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Typing :: Typed",
]
dependencies = []

[project.scripts]
pyrewind = "pyrewind.cli.main:main"

[project.urls]
Homepage = "https://github.com/adityarajIITj/PYREWIND"
Documentation = "https://github.com/adityarajIITj/PYREWIND/blob/main/README.md"
Repository = "https://github.com/adityarajIITj/PYREWIND.git"
Issues = "https://github.com/adityarajIITj/PYREWIND/issues"

[project.optional-dependencies]
dev = ["mypy>=1.8", "pytest>=7", "ruff>=0.4"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"

[tool.ruff]
target-version = "py310"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.mypy]
python_version = "3.10"
files = ["pyrewind"]
check_untyped_defs = true
warn_unused_ignores = true
warn_redundant_casts = true
no_implicit_optional = true